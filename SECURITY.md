# 安全与隐私

项目面向自己的电脑、服务器和手机。能登录网页或持有已绑定设备的使用者，可以读取同步内容并让桌面 Codex 执行任务，因此访问权限等同于远程操作你的 Codex。

- 桌面通过 SSH 主动交换数据，强制校验主机密钥；浏览器通过 HTTPS 和密码会话访问。
- 本机管道 / Unix socket 不开放到网络；没有独立远程 shell 端点。Codex 任务仍具有桌面当前授予的权限。
- 消息请求检查身份、Origin、CSRF 和目标归属。队列有幂等 ID；结果不确定时不自动重发。
- Android 配对码单次使用并绑定 PKCE 校验；服务器保存设备令牌哈希，手机密文受 Keystore 保护。
- 不复制 Codex 账号凭据。过滤工具输出和内部推理不意味着正文自动脱敏；主动写入消息的密码仍会镜像。
- 服务器持有对话明文。这不是端到端加密系统。部署前确认你信任该服务器和管理员。
- `connection.json`、运行日志、对话缓存、图片、数据库、SSH / TLS / Android 私钥均不得提交。`.gitignore` 和源文件审计提供基本防护，无法替代人工检查。
- `testdata/localhost-test-only.pem` 是公开的测试证书及测试私钥，唯一允许跟踪的 PEM 文件；永远不能用于真实服务。

发布使用全新的源码历史，不导入私人备份仓库。服务器地址、真实任务 ID、生产证书、APK 和历史日志不包含在公开发布中。

发现可能影响真实部署的漏洞时，请优先使用 GitHub 的私密漏洞报告（如果仓库已启用）。公开 issue 请只放去除凭据、对话和个人路径的最小复现，不上传运行数据。
